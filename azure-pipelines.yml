trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

steps:
- checkout: self

- script: rm -rf ~/.m2/repository
  displayName: Clear Maven Cache

- script: |
    java -version
    mvn -version
  displayName: Verify Java and Maven Versions

- script: mvn clean install -X
  displayName: Maven Build with Debug Logging
